# CityCare voice client

This is the WebRTC browser client for `../voice/bot.py`.

```powershell
cd backend/voice_client
npm install
npm run dev
```

Open `http://127.0.0.1:5174`, allow microphone access, and start the voice
session. The Pipecat server must already be running on port 7860.
